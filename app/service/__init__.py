"""Service layer for business logic.

This package contains services for login, portfolios, and users. Services call
the lower-level `db` module for persistence and expose higher-level APIs used
by the CLI presentation layer.
"""
